name: Build and Deploy Plantz
on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - '.gitignore'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Gitea Registry
        run: |
          echo "${{ secrets.DOCKER_TOKEN }}" | docker login ${{ secrets.REGISTRY_HOST }} -u ${{ secrets.DOCKER_USER }} --password-stdin

      - name: Build and Push
        run: |
          SHA_TAG="${{ secrets.REGISTRY_HOST }}/${{ secrets.REGISTRY_IMAGE }}:${{ gitea.sha }}"
          LATEST_TAG="${{ secrets.REGISTRY_HOST }}/${{ secrets.REGISTRY_IMAGE }}:latest"
          docker build -t "$SHA_TAG" -t "$LATEST_TAG" .
          docker push "$SHA_TAG"
          docker push "$LATEST_TAG"

  deploy-swarm:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Update Swarm Service
        run: |
          docker service update \
            --image "${{ secrets.REGISTRY_HOST }}/${{ secrets.REGISTRY_IMAGE }}:latest" \
            --force \
            --with-registry-auth \
            ${{ secrets.SWARM_SERVICE_NAME }}

      - name: Wait for service to be healthy
        run: |
          echo "Waiting for rollout to complete..."
          for i in $(seq 1 24); do
            STATE=$(docker service ps ${{ secrets.SWARM_SERVICE_NAME }} \
              --filter "desired-state=running" \
              --format '{{.CurrentState}}' | head -1)
            echo "  [$i/24] State: $STATE"
            case "$STATE" in
              "Running"*)
                echo "Service is up."
                exit 0
                ;;
              "Failed"*|"Rejected"*)
                echo "Deployment failed: $STATE" >&2
                docker service ps ${{ secrets.SWARM_SERVICE_NAME }} --no-trunc
                exit 1
                ;;
            esac
            if [ "$i" = "24" ]; then
              echo "Timeout waiting for service to stabilize." >&2
              docker service ps ${{ secrets.SWARM_SERVICE_NAME }} --no-trunc
              exit 1
            fi
            sleep 5
          done

      - name: Health check
        run: |
          APP_URL="${{ secrets.APP_URL }}"
          APP_URL="${APP_URL%/}"
          if [ -z "$APP_URL" ]; then
            echo "APP_URL not configured, skipping health check"
            exit 0
          fi
          HEALTH=$(curl -s -L -o /dev/null -w "%{http_code}" --connect-timeout 10 --max-time 15 "$APP_URL/api/health" 2>/dev/null || true)
          if [ -z "$HEALTH" ] || [ "$HEALTH" = "000" ]; then
            echo "Health check: curl failed (network unreachable from runner), skipping"
            exit 0
          fi
          if [ "$HEALTH" != "200" ]; then
            echo "Health check failed (HTTP $HEALTH)" >&2
            docker service ps ${{ secrets.SWARM_SERVICE_NAME }} --no-trunc
            exit 1
          fi
          echo "Health check passed (HTTP $HEALTH)"
